namespace IGX.ViewControl
{
    /// <summary>
    /// 마우스 픽킹(Picking)을 통해 3D 씬에서 선택된 단일 지오메트리(객체)를 나타내는 불변(immutable) 클래스
    /// <see cref="ModelID"/>와 <see cref="GeometryID"/>를 조합하여 특정 모델 내의 특정 지오메트리를 고유하게 식별
    /// 이 클래스는 <see cref="IEquatable{PickedItem}"/>을 구현하여 컬렉션에서 효율적인 비교와 조회가 가능하도록
    /// </summary>
    /// <param name="modelId">선택된 지오메트리가 속한 모델의 고유 식별자(ID)</param>
    /// <param name="geometryId">선택된 지오메트리의 고유 식별자(ID)</param>
    public struct PickedGeometry(int modelId, int geometryId)// : IEquatable<PickedGeometry>
    {
        public int ModelID { get; } = modelId;// 선택된 지오메트리가 속한 모델의 고유 식별자(ID)를 가져옴.
        public int GeometryID { get; } = geometryId;// 선택된 지오메트리의 고유 식별자(ID)를 가져옴.
        //public override bool Equals(object? obj)
        //{
        //    return obj is PickedGeometry other && Equals(other);
        //}
        //public bool Equals(PickedGeometry? other)
        //{
        //    if (other is null)
        //    {
        //        return false;
        //    }

        //    if (ReferenceEquals(this, other))
        //    {
        //        return true;
        //    }
        //    return ModelID == other.ModelID && GeometryID == other.GeometryID;
        //}

        //public override int GetHashCode()
        //{
        //    return HashCode.Combine(ModelID, GeometryID);
        //}

        //public override string ToString()
        //{
        //    return $"ModelID: {ModelID}, GeometryID: {GeometryID}";
        //}
    }
}