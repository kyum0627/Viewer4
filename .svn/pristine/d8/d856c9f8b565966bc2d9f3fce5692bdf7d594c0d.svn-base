using IGX.ViewControl.Render;

namespace IGX.ViewControl
{
    // 렌더링 전략 인터페이스
    public interface IRenderer : IDisposable
    {
        void Initialize(int width, int height);
        void Draw(IMyCamera camera, bool draw);
        void Resize(int width, int height);
    }
}