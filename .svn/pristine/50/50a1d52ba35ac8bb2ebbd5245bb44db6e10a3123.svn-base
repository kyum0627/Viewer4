using OpenTK.GLControl;

namespace IGX.ViewControl
{
    public interface IEventHandler
    {
        void SetupEvents(GLControl control, Action render, Action<int, int> selectObject, Action setupViewport);
    }
}
